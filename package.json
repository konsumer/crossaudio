{
  "name": "crossaudio-root",
  "private": true,
  "type": "module",
  "devDependencies": {
    "lerna": "^4.0.0",
    "standard": "^16.0.3"
  },
  "dependencies": {
    "@crossaudio/core": "0.0.10",
    "crossaudio": "0.0.10",
    "react": "^17.0.2",
    "web-audio-scheduler": "^1.4.0"
  },
  "scripts": {
    "start": "lerna run dev --stream --scope crossaudio-site",
    "deploy": "lerna run deploy --stream --scope crossaudio-site",
    "postinstall": "lerna bootstrap"
  }
}
