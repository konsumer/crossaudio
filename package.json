{
  "name": "crossaudio-root",
  "private": true,
  "type": "module",
  "version": "0.1.19",
  "scripts": {
    "start": "npx -y lerna run dev --stream --scope crossaudio-site",
    "deploy": "npx -y lerna run deploy --stream --scope crossaudio-site",
    "postinstall": "npx -y lerna bootstrap",
    "build": "npx -y lerna run build --stream",
    "publish": "npx -y lerna publish",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "web-audio-scheduler": "^1.4.0"
  }
}
