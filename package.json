{
  "name": "crossaudio-root",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx lerna run dev --stream --scope crossaudio-site",
    "deploy": "npx lerna run deploy --stream --scope crossaudio-site",
    "postinstall": "npx lerna bootstrap"
  }
}
