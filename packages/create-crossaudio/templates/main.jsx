import React from 'react'
import ReactDOM from 'react-dom'
import './style.css'

const App = () => (
  <div>
    Sorry, I haven't setup crossaudio react yet, but this shows the template is working.
  </div>
)

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
)
